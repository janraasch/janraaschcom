baseURL = "https://www.janraasch.com/"
languageCode = "en-US"
title = "Jan Raasch, full stack developer building software for product value 🚀"
theme="hugo-bearblog"

# Generate a nice robots.txt for SEO
enableRobotsTXT = true

# Generate "Bearblog"-like URLs !only!, see https://bearblog.dev/.
disableKinds = ["taxonomy", "404"]
ignoreErrors = ["error-disable-taxonomy"]
[permalinks]
  blog = "/:slug/"
  tags = "/blog/:slug"

[params]
  # Metadata for search engines and social sharing
  author = "Jan Raasch"
  description = "Jan Raasch. Hi there, my name is Jan. I’m a software developer with a passion for building (digital) products. I have 10yrs+ of experience working as a professional software developer in the industry. In another life (10yrs+ ago) I used to be a Mathematician. For more details check out my resume. I’m happy to talk about work engagements, reach out!"

  # Another "title" :-). This one is used as the site_name on the Hugo's internal
  # opengraph structured data template.
  # See https://ogp.me/ and https://gohugo.io/templates/internal#open-graph.
  title = "Jan Raasch, full stack developer building software for product value 🚀"

  # The path to your "favicon". This should be a square (at least 32px x 32px) png-file.
  # Hint: It's good practise to also put a "favicon.ico"-file into your "static"-folder.
  favicon = "images/favicon.png"

  # These "images" are used for the structured data templates. This will show up, when
  # services like Twitter or Slack want to generate a preview of a link to your site.
  # See https://gohugo.io/templates/internal#twitter-cards and
  # https://gohugo.io/templates/internal#open-graph.
  images = ["images/share.png"]

  # This theme will, by default, inject a made-with-line at the bottom of the page.
  # You can turn it off, but we would really appreciate if you don’t :-).
  # hideMadeWithLine = true

[markup]
    [markup.goldmark]
        [markup.goldmark.renderer]
            hardWraps = true
